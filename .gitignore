# graphify: local-only artifacts.
# graphify-out/ itself IS meant to be committed (graph.json, GRAPH_REPORT.md,
# graph.html) so the team shares one map — only these local bits are excluded.
graphify-out/cost.json
graphify-out/cache/
graphify-out/.graphify_*
